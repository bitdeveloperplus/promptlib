{
  "name": "@packages/database",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "predev": "npm run db:generate",
    "dev": "tsup ./src/index.ts --watch",
    "db:pull": "prisma db pull",
    "db:generate": "prisma generate",
    "db:seed": "npx ts-node src/seeds/index.ts",
    "build": "tsup ./src/index.ts"
  },
  "devDependencies": {
    "prisma": "^5.8.0",
    "tsup": "^8.0.1",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.8.0"
  }
}

